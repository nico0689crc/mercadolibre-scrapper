# URL del backend usada desde el servidor (Server Components / Route Handlers).
# En Docker, compose la pisa con http://backend:4100/api (red interna).
API_URL=http://localhost:4100/api

# URL del backend expuesta al browser (Client Components). Se inlinea en build.
NEXT_PUBLIC_API_URL=http://localhost:4100/api
